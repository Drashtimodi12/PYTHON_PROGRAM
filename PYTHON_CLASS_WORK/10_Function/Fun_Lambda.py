# Lambda Function (One-Line Function)
# A lambda function is a short function that does not have a name.

# Creating a lambda function to multiply two numbers
multiply = lambda a, b : a * b 

# Calling the function with arguments 10 and 2
print(multiply(10, 2))

# Output: 20